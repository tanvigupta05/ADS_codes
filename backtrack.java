//backtracking

public class backtrack {
    // backtracking is a technique used to explore all possible solutions by trying out differnet choices and undoing when choice leads to a dead end.
    // recursive approach
    // if condition met, store the result.
    // if the choice does not lead to solution undo the last choice(backtrack)
}
